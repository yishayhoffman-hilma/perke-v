client:

login register-using post to get if user with password fields exists 
landing page with main directory - displays the main user directory
navigating between files/directories - updates the url to the current folder/file
functions - info,show,rename,copy,move,delete

server:

contains in users/ folder a folder for every user with their data 
express methods based on url (get,post,delete,put)
handles user requests
sends status notifications to client

